#include "main.h"
#include <string.h>
/**
 * _strlen - returns the length of a string
 * @s: the string
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
